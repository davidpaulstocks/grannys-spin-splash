/** ComboTracker unit tests — combo count + 2s decay window + multiplier. Populated alongside ComboTracker.ts. */

import { describe, it } from 'vitest';

describe('ComboTracker', () => {
  it.todo('increments combo on consecutive hits');
  it.todo('resets combo after the 2-second decay window');
  it.todo('multiplies stars awarded by current combo');
});
